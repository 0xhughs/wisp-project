# slices/

Archive accepted BUILD pages as `NN-slug.md` only after independent Reviewer approval of the recorded contract and candidate identity. Preserve final evidence, review events and counter totals. Existing archives are immutable; later fixes receive their own slice.

Coordinator alone archives and advances using LOOP.md. Interrupted advances reuse verified matching archives; differing history requires reconciliation. Open history only when current dependencies, regression analysis or recovery require it.
