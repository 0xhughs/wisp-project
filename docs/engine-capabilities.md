# Pinned capability map

Source: [DeepSeek Harness d347e703908d0406b7a7ef80e3a0e594d86b2215](https://github.com/deepseek-ai/deepseek-harness/tree/d347e703908d0406b7a7ef80e3a0e594d86b2215). Paths and line references below refer to that immutable revision, inspected in the pristine local clone. **Verified** means directly exercised by this spike, **conditional** means source-supported but awaiting the stated Wisp adapter and proof, and **unavailable** means excluded from this composition or not supplied by the upstream seam. Retaining a service is not verification of its whole feature set.

| Capability | Pinned source / actual seam | Proposed Wisp integration and permission/UI implications | Evidence and status |
| --- | --- | --- | --- |
| Models / providers | `packages/llm/llm-pi-ai/src/provider.ts:167` `buildProvider`; `config.ts`, `catalog.ts`, `adapter.ts`; base row `llm-pi-ai`; SDK `server.ts:135` initialize. | Wisp Models manages route metadata and credential references separately from speech engines. Slice 08 adds a Wisp-owned hardware snapshot and Faster/Recommended (`qwen3:8b`)/Stronger rows plus the existing DeepSeek cloud alternative. Inspect is GET-only against the saved loopback Ollama; download stays gated on a consented resource plan. Surface capability errors and compatibility status. Preserve compatible provider implementations. | **Verified narrowly (05):** custom openai-completions route to local Ollama 0.33.3/Qwen3:8b, off→none mapping, streamed native tools and final text. Preflight checks runtime version, model digest, actual loaded 8192 context and three terminal streams. Four live Harness cases passed. **Linux-supplemental (08):** closed hardware schema/collector/recommend and loopback Ollama inspect fixtures. Native IOKit/Metal/Settings onboarding is **uncompiled on Linux**. Other providers/models remain **conditional** on route, credentials, protocol and safety tests. No hardware/speech-headroom numeric promise. |
| Plugins | `packages/boot/app-boot/src/profile.ts:197` `initProfile`, `:685` read manifest, `:706` write manifest, `:805` load profile, `:854` compose entries; `packages/bundle/base/cordis.patch.yml` inventory row. | Wisp Plugins owns install/configure/remove/status for the covered demonstration using product `--patch` overlay + process replacement. No Harness management UI, `dsh plugin` or marketplace. | **Linux-supplemental:** closed catalog, classifier and overlay composition for `wisp-compatible-plugin`. Isolated initialize of that insert is **missing-external-resource** until a prepared pin runtime exists. The 01/06 product overlay loader seam remains the verified mount path. General third-party install/update/remove remains **conditional**. Native Plugins UI is uncompiled on Linux. |
| MCP | `packages/mcp/mcp-client/README.md:11`; `src/index.ts:146` async apply/startConnection, readiness and disposal; base does not mount servers. | Friendly Connections and advanced custom MCP configure namespaces, stdio/HTTP transport, credentials, startup failure and cancellation. Require scoped confirmations before consequential tools. | **Conditional:** source bridge registers external tools; no MCP server was connected. All such tools unavailable in the spike inventory. MCP resources/prompts are **unavailable in this pinned tool-only bridge**. |
| Skills | `packages/skill/skill/src/index.ts:358` `SkillRegistry`, `:392` registerProvider; `skill-filesystem`, `tool-skill` base rows. | Wisp Skills manages reusable behaviors belonging to the same companion. Validate installed content and inherited action permissions; management UI is distinct from execution. | **Conditional:** registry retained, filesystem discovery/instructions and invocation tools explicitly disabled. No skill was installed or executed. |
| Tools | `packages/core/tools/src/index.ts:1101` `guard`, `:1471` approval branch, `:1680` `serviceAsk`; base tool rows. | Registry remains capability authority. Wisp translates immutable actual call identity to friendly permissions. New tool schemas require safety/compatibility tests. | **Verified:** actual registry exposes exactly `wisp_test_effect` at initialization; real registry unit tests and live 0/0/0/1 effect cases. Shell, filesystem, web, jobs, goals, workflow and delegated tools **unavailable in this profile**, preserved upstream for future gated integration. |
| Sessions / events | `packages/sdk/server/src/server.ts:75` server, `:135` initialize, `:246` handleRequest; `packages/core/session/src/index.ts`; `packages/session/session-persistence-jsonl/src/index.ts:219` create. | One persistent Wisp identity will attach sessions/state internally. Future clients consume correlated completion events, not queue receipts. Never make session transcripts the product's main interface. | **Verified:** fresh session, exact message receipt/turn correlation, nonempty final text, cancellation, buffered pre-reply frames, rejection of stale pre-turn/pre-end idle, and durable turn events after root disposal, including cancelled approval pairs and aborted turns for EOF/SIGTERM while approval is pending. The root disposal interposition is a pinned private seam; other upstream revisions require fresh lifecycle verification. Resume across product launches and identity migration remain **conditional**. |
| Storage | `packages/storage/storage/src/index.ts:47` `Storage`, `mount`, `form`, `domain`; base `storage-json`/`storage-domain`; session persistence package above. | Attach to a user-chosen Wisp directory, with understandable/editable product memory separated from diagnostics; define migrations and failure handling. | Durable session JSONL is **verified** through its own backend. General KV/domain storage and product memory are **conditional**: retained services, no standalone domain round-trip or memory implementation. |
| Credentials | `packages/credentials/credentials-local/src/index.ts:513` `LocalCredentialProvider`; managed writes at `:699`, `:720` use 0600 files / 0700 dirs; base credentials row. | Wisp must manage protected credentials, never put them in ordinary memory or diagnostics; future platform credential protection and migration need proof. | **Verified narrowly:** isolated environment with explicit public loopback placeholder supports this route. Real credential creation/rotation/deletion and at-rest protection are **conditional** and not tested. The provider's managed YAML is not a claim of OS keychain encryption. |
| Approvals | `packages/interaction/user-approval/src/index.ts:142` `ApprovalService`, `:207` request; core tools `serviceAsk`; Wisp `approval-bridge.ts`. | Own the entire user-facing approval surface. Show immutable actual action, bind nonce/session/call/digest, allow once only, deny/cancel fail closed. | **Verified for one harmless tool:** withhold, deny, cancel, allow, wrong identity/digest, replay, changed arguments, deadline, missing answerer and both cancellation/effect orderings. Actual durable asked/decided IDs paired after disposal. Global delegated-route enforcement is **conditional** and belongs to slice 06. |
| Sub-agents | `packages/subagent/subagent/src/index.ts:190` `SubagentRuntime`, `startContinuable`; base in-process spawn/fork providers and tool rows. | Internal delegation remains the same Wisp identity; inherit action-scoped permissions and lifecycle controls. External SDK/CLI providers require their own runtime and licensing assessment. | **Conditional:** runtime/providers retained but all sub-agent invocation/control/list tools disabled. No delegation executed; no permission propagation claim. |

## Composition and inspection evidence

Inspected `packages/bundle/base/cordis.patch.yml` and `packages/bundle/sdk-app/cordis.patch.yml` in full. Inspected SDK server, pi-ai README/config/catalog/provider/adapter, app-boot profile functions, MCP README/public entry, skill registry, core tools and ApprovalService, session persistence, storage hub, credentials-local and sub-agent runtime. The inspection commands were `rg --files packages | rg '/mcp'`, targeted `rg -n` for exported services/functions and `serviceAsk`/`guard`, and `sed`/`cat` of those pinned paths (working directory is the pristine clone). Local inspection output is retained at `work/loop-state/01-build-008-source-inspection.log`; it includes one unsuccessful guessed tools/registry.ts lookup, subsequently corrected to core/tools/src/index.ts. No source claims rely on the missing path.

The exact restrictive overlay is `../spike/profile/wisp.patch.yml`. It explicitly disables stock transport, HMR, telemetry, title LLM, all general-purpose executable tool rows and ambient instructions/skill discovery/extra-turn drivers. `DSH_TELEMETRY_DISABLED=1` supplements the disabled telemetry row because upstream feedback-only defaults can release raw records. Underlying session/settings/credentials/storage/skills/sub-agent registries remain. The dumped profile is inspection evidence only: `!!js` expressions are not evaluated there. Runtime inventory and four live cases independently verify the one-tool gate. Wisp does not rely on its persona to enforce this boundary.

## Source and distribution findings

The inspected root `LICENSE` identifies DeepSeek copyright 2026 and MIT terms, including preservation of its copyright/license notice and warranty disclaimer when copying substantial portions. Vendored Cordis and foundation packages retain their individual MIT notices (`vendor/*/LICENSE` and `vendor/README.md`). This spike keeps the upstream clone and lockfile external, without distributing a packaged app.

`THIRD_PARTY_NOTICES.md` explicitly lists direct workspace dependencies and a disclosed Claude Code platform-payload closure; it is not a complete transitive attribution inventory. `pnpm-lock.yaml` is the npm transitive authority and `python/sdk/uv.lock` is separate. The notices include MIT, Apache-2.0, BSD and ISC dependencies, plus `@anthropic-ai/claude-agent-sdk` and official platform payloads declared with `SEE LICENSE` terms. The upstream owner's statement about official Claude Code payload distribution is not treated as authorization for Wisp or as a blanket permissive license. Locally patched packages include `node-pty@1.2.0-beta.15` and `@yao-pkg/pkg@6.21.0`; packaging must preserve notices and review modifications/actual bundled closure.

**Packaging readiness is not demonstrated.** Before any distribution, determine the exact shipped runtime/native/model closure, review each applicable term and notice, produce attribution/SBOM artifacts, and separately assess Ollama and model weights if bundled. Downloading a local test model does not authorize redistribution. The current proof supports local spike feasibility only, not signing, publication, commercial licensing advice or a fully compatible product.
