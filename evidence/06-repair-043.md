# Slice 06 repair043 — review comparison

Independent review042 rejected candidate `6cb3ca2a4aca08f0833b823dcd7c5ef90b2c01c1434181bb6f66453d6c01db6f` under unchanged contract `7dd5e000e5bb4b45a50a7b3b65ce68364319c1312c96ade9f2a312861822ba5f`. This is the first repair, with received counters1 rejection/0 no-progress. Builder does not consume verdicts or grant acceptance. Full independent findings are external `work/loop-state/06-review-042.md`.

External repair artifact root `R = work/wisp-06/06-repair-043`. Prior proof in `06-permissions.md` is retained history, supplemented by the fresh evidence here.

| Review gap | Before | Repair and headless proof |
| --- | --- | --- |
| Native close of two pending requests shuts down connection | Reviewer GUI run1788984114378 sent two cancels. First SDK cancel closed all; second stale cancel caused unavailable/bridge exit1, dropping closure receipts. No unintended effect. | Native `PermissionState.cancelAll()` emits one cancellation batch using existing SDK operation scope. All pending IDs become deciding before transmission, so repeat close/Cancel cannot send another decision. Controller uses this seam. `PermissionCloseProbe.swift` applies two close gestures and emits exactly one decision. `permissions-lifecycle.mjs --native-close-probe` feeds those actual native decisions into the actual bridge/registry, cancels two successive queues on the same generation, receives all four matched cancelled closures, has zero effect and no unavailable, and exits cleanly. |
| Permission fixture/failure falsely marks Models verified | Reviewer observed unavailable → deterministic permission-test/modelCalls0 → generic tested; native transient flag had been cleared, so Models claimed a completed response despite zero model tests. | Bridge starts/results/completions bind operation type, unique ID and generation. Native `BridgeOperationState` requires a matching actual connection-test response with nonempty completed turn and zero effects. Generic tested only settles the matching operation's busy state; it cannot mark verified. Failure/restart/home invalidation clears the operation. Native tests cover fixture exclusion, failure followed by trailing completion, stale generation, wrong operation ID and replay; actual matching connection completion remains supported. Bridge suppresses success completions after failure/ending. |
| Lifecycle assertion races closure monitor | Reviewer exact-copy test failed0≠2 at permission-test arrival; matching closures arrived later. Independent adjusted oracle passed. | Submitted test now separately awaits all matching closures within a bounded deadline and checks request ID/generation/session/call/digest/outcome. Every lifecycle scenario checks exact correlated closures and no success completion after failure. Bridge explicitly drains pending approval notifications before completion and after runtime shutdown, preserving final receipts. R/lifecycle02 passes six scenarios without model inference. |

## Checks

- `R/native-red.log`: new native regression checks fail before cancellation-batch/operation-state implementation. The compiler stopped at the missing `BridgeOperationState` type; independent reviewer GUI/event artifacts provide the original cancellation and false-status behavioral failures. Both new native regression groups pass after implementation.
- `R/native01.log`: full native assertion runner passes after repair, including cancellation coalescing, pending ID coverage, operation/failure/generation correlation, existing home/memory/models/isolated Keychain/frame checks. Legacy Keychain deprecation warnings are retained.
- `R/js-tests.log`:36 desktop JavaScript and accepted spike client tests pass.
- `R/build01.log`: fresh native application builds successfully.
- `R/lifecycle01`: initial enhanced native-close + existing five lifecycle cases passed. `R/lifecycle02`: adds exact closure correlation to every case and no completion after failure; six cases pass. Both use actual pinned registry/bridge, zero model inference and owner-only harmless ledgers. Native-close is two queue batches/four closures; other scenarios are one batch/two closures. Invalid frame/stale-generation remain failures that deny all effects and release owned processes.

Commands from workspace root:

```
outputs/wisp-project/desktop/scripts/test-native.sh <R/native01>
node --test outputs/wisp-project/desktop/tests/*.test.mjs outputs/wisp-project/spike/tests/client.test.mjs
swiftc outputs/wisp-project/desktop/macos/Sources/WispBody/PermissionState.swift outputs/wisp-project/desktop/tests/PermissionCloseProbe.swift -o <R/PermissionCloseProbe>
node outputs/wisp-project/desktop/tests/permissions-lifecycle.mjs --scratch <R/lifecycle02> --runtime-root <work/wisp-01/builder-01> --native-close-probe <R/PermissionCloseProbe>
outputs/wisp-project/desktop/scripts/build-macos.sh --scratch <R/build01>
```

No SDK approval-policy change or stale Allow/Deny relaxation was made. The unaffected34 registry, six actual local direct/plugin, four actual child, sentinel transport and sanitization checks passed independently in review042; they are not falsely reported as new repair runs. Repair targets bridge/native cancellation and operation completion plus the lifecycle oracle. Accepted body/home/memory source remains unchanged; prior041 actual body/memory continuity observations remain relevant but do not replace fresh required native action checks. No cloud calls or real-key access, no protocol/archive edits, no accepted spike or tracked upstream changes.

## Fresh native/model proof after repair

Coordinator `/root` alone actuated native input through the existing CUA session. Exact build01 native6501 was verified before selection. External `work/loop-state/06-repair-043-root-ui.md` records the input observations; correlated private events and final runner result are in `R/gui/run-1788984929143`. AX/screenshots were observed in tool output, not saved as PNG artifacts. This is Builder-phase coordinator input evidence, not independent approval.

1. Two deterministic actual-registry requests pending: actual window close cancelled both, pending2→1→0, then completed2/allowed0/modelCalls0. Both ledgers remained empty. Same bridge6502/runtime6509/generation stayed usable and idle. Models showed Attached, connection has not been tested. This directly reproduces and resolves review042's ordinary-close failure.
2. Actual Ollama native direct request: Cancel was initially focused; Tab twice and Space explicitly chose Allow Once. Turn1/approvalCount1/effects1. Native plugin request: resize660×572→522×422 forced detail overflow; actual scrollbar moved0.1→1, with the bottom record label and once-only scope visibly accessible. Tab/Deny/Return produced turn2/effects0. A subsequent actual direct request was cancelled with Escape, turn3/effects0/nonemptyfalse as expected for interruption. Actual plugin Allow Once produced turn4/effects1. Every operation had its own recorded UUID, one approval and its exact ledger outcome; no model noncompliance retry was used.
3. After all four permission operations, Models still showed Attached, connection has not been tested. A real Test Connection produced matching `operation:test`, ID `ced7709a-fa38-4de0-a058-f353895283fd`, generation `e409baf7-67fb-4876-8a0c-c81d51e4b3cf`, completed nonempty response/effects0; only then did native Models show Verified.
4. A fresh two-pending queue was cancelled by actual Models Save and Apply. Both matching cancelled receipts arrived; old bridge6502/runtime6509 released. New bridge6677/runtime6682, generation `267996cb-d8ad-4c78-a908-0976e8de9c8e`, retained companion and memory. Models reset to Attached/not tested. A genuine connection test with ID `6fa92474-adc1-4feb-aa91-0b70731c8f08` then completed nonempty/effects0 and changed Models to Verified.
5. A fresh queue remained pending when actual Command-Q quit. Both cancelled receipts were observed, bridge exit0; runner exit0/cleanuptrue/noForcedStoptrue, two generations and two completed connection tests. All five owned PIDs6501/6502/6509/6677/6682 were absent afterward. No CUA query followed Quit. Original ledgers contained exactly direct1/plugin1 from the explicit model approvals; new-generation ledgers were both empty.

All three reported gaps materially improved: ordinary native cancellation no longer destroys the connection or drops receipts; permission/failure completions cannot claim model verification while actual connection responses still can; the submitted lifecycle test now passes with a bounded, exact closure oracle. Required Builder repair proof is complete and all owned work is stopped for fresh independent review. Contract/counters are not modified by Builder. The unaffected route/security and body/memory proof remains explicitly distinguished above; no cloud or real-key access occurred.
