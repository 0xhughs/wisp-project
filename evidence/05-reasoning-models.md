# Slice 05 Builder evidence — 05-build-032

Candidate implements the approved compatibility subset. Builder cloud continuation05-cloud-034 subsequently completed the mandatory real cloud greeting, recall, stored-key restart recall and return-local proof using three explicitly authorized attempts. Independent implementation review is still required. The original05-build-032 checkpoints below are historical and their cloud-pending statements are superseded by the continuation record at the end and evidence/05-cloud-results.json. Disposable sentinel initialization and loopback fixtures remain distinct from actual external cloud evidence.

## Implementation and criterion mapping

1. Native Models exposes local identifier/loopback endpoint, pinned DeepSeek model choices, secure credential entry/status, explicit remove confirmation, Save and Apply, Revert Draft, Reload Saved and fixed Test Connection. Saved/active/attached-but-untested/testing/verified/failure states differ. Real UI reached Models through Settings and status Change Model, edited invalid endpoint and reverted, persisted selection, masked and cleared sentinel field, canceled then confirmed key removal, and recovered an external configuration conflict. The conflict recovery copy was corrected to Models/Revert Draft/Reload Saved after the actual conflict checkpoint; native assertion verifies that final copy. General/memory remain usable when reasoning configuration fails. Speech is unavailable.
2. Actual selected local qwen3:8b completed fixed tests and generated-fact recalls through native → product bridge → immutable accepted adapter → installed Harness → existing Ollama. Cloud missing key prevented launch; sentinel cloud initialization across restart tested actual stored-key retrieval without any prompt. Local recovery then completed turns without cloud key. **Actual DeepSeek completion and cloud→local completion after it remain missing.**
3. Controller generation guards and observed old bridge/process-group exit gate replacement. Actual GUI applied during an in-flight local test, changed to broken loopback port, observed safe failure, corrected and recovered, restarted application and retained companion UUID/home/saved route/fact. Saved memory attached on reasoning restart while an unrelated unsaved memory draft survived Models navigation and apply. All four model GUI runs quit with no orphan or forced stop. Cloud recall remains missing. Native assertions exercise state interruption/staleness/failure; real body fault runners exercise process death. No claim of real cloud failure or every possible OS scheduler interleaving.
4. Real isolated macOS Security add/read/replacement/remove, exact namespace canary, restart read, denied-operation injection, transaction interruption before/after key/config commit, locked-store refusal/unlock recovery passed. Actual GUI sentinel save/clear/restart/remove/cancel-remove passed in explicitly isolated Keychain. Versioned exact IDs and pending cleanup journal avoid unrelated deletion. A locked read initially hung inside Security despite interaction suppression; the exact disposable test process was stopped. Metadata-only lock preflight now refuses before item lookup and the actual lock/recovery test passes. Production per-item OS authorization can still be required off the main thread; no default search-list modification or personal item enumeration occurred. A real user credential in production-mode scoped Keychain remains part of cloud resume.
5. Native and JS schemas reject unknown versions/fields, unsafe endpoint/model/bootstrap and forged reference paths. Actual config link/ownership/corruption and transaction tests preserve canaries. Real installed DeepSeek provider exercised against a test-only loopback HTTP endpoint: two requests total, exact intended Authorization/model, max_tokens1024, no retries, missing fixed reference no ambient fallback. SDK raw error frames can contain hostile echo in trusted memory; product bridge never serializes them. A separate actual product bridge hostile-error/duplicate-bootstrap/missing-key test passed with one loopback request, zero external requests, no sentinel in product output or scanned artifacts. Initial bridge shutdown timeout was fixed by destroying its owned stdin on finish; RED→GREEN recorded. Existing memory-security runner passed all14 isolation/ownership/assembled-input/effect/redaction checks. Accepted spike and archives are byte-identical to baseline.
6. Final native assertion build/run and final app build passed; final JS suite18 passed. Body live regression passed seven cases. Final real shell normal runner passed14 checks: all11 sections, two distinct turns in same controller/engine/session, body recreation, same Settings window close/reopen, one menu owner, body never key/main, voice unavailable and clean Quit. Separate owned ClickTarget with authorized InputProbe proved fixed typing, transparent-corner click and scroll reaching target, and visible-body drag after model editing/recreation. Final real chooser reselected the generated home after an actual filesystem move, retained UUID/fact and completed local recall, then clean Quit. Native real-file home relocation/conflict/ownership tests also pass. Remaining cloud gates cannot be replaced by these checks.

## Source and security provenance

Pristine Harness pin d347e703908d0406b7a7ef80e3a0e594d86b2215 rechecked clean. The actual installed pi-ai0.84.2 catalog and resolveProfiles are checked by models-source.mjs. Models are deepseek-v4-flash/pro at fixed https://api.deepseek.com; catalog membership is not account availability or pricing. Source evidence is retained in approved BUILD Tests and 05-plan-031. Product configuration is desktop/engine/reasoning-config.mjs; normal product no longer imports an arbitrary route file. Authorized local route remains hash5afb9e4971433c059e1ec76ea772da8527981ad9ccdcccf951dc2543a1571f5d.

Cloud secret moves from exact companion service/account Keychain lookup into one bounded anonymous bootstrap pipe, then only the selected fixed child credential environment reference. Local does not read cloud key. Secret data is absent from argv, ordinary route files, memory, generated YAML and product error events. Trusted native/bridge/SDK/child process memory can hold it; no same-user-debugger/root protection or cryptographic zeroization promise. Raw SDK failures must never be serialized. Test legacy Keychain APIs are deprecated on this SDK; their observed behavior is documented rather than an unsupported iOS protection-class claim.

## Reproduction and artifacts

Paths below are relative to the shared workspace (two levels above repository); generated outputs remain outside source coverage.

- work/loop-state/05-build-032-native-last.log: actual final native assertions; build-last.log: final assembled app; js-last.log:18 tests.
- work/loop-state/05-build-032-source-profile.log; wire2.log; bridge-security2.log; body-regression.log; memory-security.log.
- work/wisp-05/05-build-032/wire2/sanitized-results.json and bridge-security2/sanitized-results.json: distinct trusted SDK versus product error oracles.
- work/wisp-05/05-build-032/body-regression/live.json; memory-security/result.json; shell-final/result.json.
- work/wisp-05/05-build-032/live/run-1788915599998, run-1788915856357, run-1788917033783, run-1788917843623: generated private events and cleanup results. No personal test data. Sanitized aggregate is evidence/05-sanitized-results.json.
- Last model run PID92970/session18000 and final shell PID92882/session60783 exited0 with cleanup=true. Owned ClickTarget PIDs92633/92648 were terminated. No Wisp instance remains. Parent-owned Ollama/runtime unchanged.

CUA screenshots/AX were inspected only for live owned test windows; no screenshot file artifact is claimed. Actual InputProbe posting permission was checked true. CUA must never select the stopped bundle because it can launch without isolation. Start an exact runner and verify live PID first; after Quit use runner/ps only.

## Mandatory resource and exact resume

Coordinator must obtain user-authorized DeepSeek API key, access to one declared model, and bounded request/output allowance. Do not search for a key, create an account, purchase credits or infer permission from local tests. Proposed allowance: six fixed harmless cloud calls total, three Builder plus three independent Reviewer, each at most1024 output tokens and zero retries. Each role can run one greeting, one generated-fact recall and one restart/stored-key recall, then switch back to local and prove local completion. Provider rejection consumes an attempt; stop and report rather than silently retry or substitute models. Model availability mismatch requires contract review.

Once authority is supplied, create a fresh marked cloud-live directory and generate support/home using the existing HomeFixture helper; retain only generated nonsecret memory such as a verification phrase. Start models-live.mjs with --credential-store production (explicitly omits --test-keychain), prepared runtime, fresh scratch and final app executable. Verify live PID, open that exact Wisp bundle, and let the user type the real key directly into NSSecureTextField. Do not read/copy it into tools, clipboard, fixtures, disposable test Keychain or evidence. Normal production credential service is com.wisp.reasoning.<fresh companion UUID>, exact opaque account ID. Save and Apply does not send a prompt; Test Connection is the deliberate request. After proof use UI to return local; any cleanup of that exact stored real-key item must match the user's authorized retention/removal preference. No account key revocation is performed.

## Authorized cloud continuation — 05-cloud-034

User directly entered the real key into Wisp before dispatch and authorized testing/continuing against the proposed total six-call cap. Ledger work/loop-state/05-cloud-call-ledger.json recorded each Builder attempt before dispatch and its outcome afterwards. Builder used exactly3/3 attempts, leaving3 for the independent Reviewer. Each request profile caps output at1024 and configures maxRetries0; the unchanged prior wire fixture proves actual transport fields. These live outcomes are correlated SDK completed turns, not raw HTTP packet capture or a provider billing statement. No failed attempt or retry occurred.

The live UI had user-selected deepseek-v4-pro, a supported catalog model, so it was preserved. Only generated phrase “silver orchard 034” was saved through Memory. Same companion899e2b5d-dae8-44f8-b47f-4c50657e44c9 used these actual routes and outcomes:

1. UI applied existing local qwen3:8b; local recall matched generated memory (session076b8e39, turn1).
2. UI selected and applied saved DeepSeek cloud. Old local bridge97994/runtime97999 were absent before cloud testing. GUI Test Connection completed nonempty greeting (sessiona7def5d8, turn1).
3. Fixed cloud recall matched the generated phrase on that same session, turn2, effects0.
4. Actual Quit reaped all first-run generations. Exact same support/home restarted in production Keychain mode, without key entry/import. New native controller/internal session, same companion UUID and memory revision. Cloud recall matched again (sessionde8bb415, turn1), proving real retained-key retrieval and cloud memory continuity.
5. UI returned to local qwen3:8b; cloud bridge98106/runtime98112 were absent. Local recall matched again (sessionec5523cb, turn1). Native status showed local Saved/Active and verified connection. Actual status-menu Quit exited0, cleanuptrue, no forced stop. All owned PIDs absent.

No agent read/exported/copied the real key, clipboard or Keychain secret. Product used the exact scoped stored item. Cloud key remains stored as authorized, with local selected; no removal or account revocation was performed. Support/home remain work/wisp-05/cloud-live-033 for Reviewer. First runner74541 belonged to coordinator, which relayed exactly the two pre-agreed recall commands after route verification; second runner33951 belonged to Builder. Both are stopped. Run evidence: run-1788976020199 and run-1788976446707 under that scratch, private generated-memory events plus cleanup results. Sanitized completed turns and identities are in05-cloud-results.json.

No application/test/spike change was needed for live success. Bundle resources were byte-compared to covered engine/spike sources; native binary matches the final compiled artifact, SHA2565e698f316481c9539d69c072adca7b1f50e0ba67ecb5102a0534db51432363f7. Prior native/local/security proof applies to the identical binary and sources. Only evidence/docs changed in this continuation. Full input/candidate manifests are work/loop-state/05-cloud-034-before.json and after.json. The cloud resource blocker is resolved for this authorized bounded review; acceptance awaits independent implementation review, which must remain within its reserved3 attempts.

## Focused review repair

Independent05-review-035 rejected only Models keyboard traversal. Builder05-repair-036 repaired explicit enabled/visible focus and action activation, with actual keyboard-only local/cloud-form/disposable-key confirmation proof and zero cloud requests. See05-keyboard-repair.md. Fresh independent review remains required; cloud-source applicability is explicitly compared there.
