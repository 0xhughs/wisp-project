# 03 management and menu-bar shell — proposed Builder proof

Dispatch03-build-020; approved contract6045d3fd831afe95bc1647313d4c94de9f0c7c8872fadbfdcf8929e9d7ac810c. This is Builder evidence, not independent acceptance or a release claim.

Test host: macOS26.5.2 build25F84, arm64, Swift6.3.3, SDK26.5; one3440×1440-point display at scale1. The accepted Harness source remains pristine at d347e703908d0406b7a7ef80e3a0e594d86b2215. The existing verified runtime and authorized local route were reused, with no installation, model download or upstream rebuild. No TCC, Reduce Motion or other system preference was changed. Product code sends no synthesized input.

## Commands and automated outcomes

Run from the project root. Private scratch root for this dispatch is `../../work/wisp-03/03-build-020/`; substitute an absolute fresh external path when reproducing.

- `desktop/scripts/build-macos.sh --scratch <absolute-scratch>/build`: passed; runnable accessory app assembled. An initial Swift compile found the inherited `NSWindowController.owner` name conflict; the final weak back-reference is `companion` and the subsequent build passed.
- `desktop/scripts/test-native.sh <absolute-scratch>/native-tests`: passed dependency-free same-source native assertions. Management tests were first run before implementation and failed on missing ManagementState, then passed after implementation. Existing body generation/stale presentation, geometry and framing checks remain included.
- `node --test desktop/tests/body-bridge.test.mjs spike/tests/client.test.mjs`:12 passed,0 failed.
- `node desktop/tests/body-live.mjs --runtime-root ../../work/wisp-01/builder-01 --route-file ../../work/wisp-01-authorized-route.json --scratch <absolute-scratch>/body-regression --app <absolute-scratch>/build/Wisp.app/Contents/MacOS/WispBody`: all seven cases passed (normal, native signal/kill, bridge signal/kill, engine kill, startup failure). Same session, distinct turns, stale presentation clearing, hide/show and no-orphan regressions remain covered. Intentional engine/bridge/native kills are fault tests, not claimed clean engine exits.
- `node desktop/tests/shell-live.mjs --case <case> --runtime-root ../../work/wisp-01/builder-01 --route-file ../../work/wisp-01-authorized-route.json --scratch <fresh-scratch> --app <absolute-scratch>/build/Wisp.app/Contents/MacOS/WispBody`: five final-binary cases passed with real operator GUI actions: normal (`shell-final`), bridge-loss, engine-failure, missing-config, repeat. Each `result.json` records the checks; each `events.jsonl` preserves private native observations. `shell-normal` was preliminary and is not final proof.

`03-sanitized-results.json` preserves safe outcomes/counts. Raw private traces include internal live identities but no product diagnostics exposes them. The final normal two turns had zero effects and no browser processes or TCP listeners. All five final shell cases exited0 through actual status-menu Quit without a forced bridge deadline and with no remaining owned bridge or runtime process group.

## Actual GUI observations against Done when

| Criterion | Actual observation and evidence |
|---|---|
| 1 — menu/availability | Native status item was retained once at observed frame3039/1410/51/30. Authorized global click3064/15 opened an owned Wisp popup at3035/31/267/224, layer101. Actual menu pointer Change Pet, Change Model, Settings, Memory and Quit produced the expected destination/exit. Clicking the disabled voice row regions did not start listening, display a microphone or change state; voice remained unavailable. Menu titles/enabled flags are set in native code. CUA did **not** expose the popup's AX contents or capture its image; disabled row label rendering is therefore supported by source plus inactive actual clicks, not a claimed AX or screenshot readback. |
| 2 — one management window | CUA pointer selected all11 destinations in order, verifying section-specific native text; arrow Up/Down changed selection and content. Ten real Command-W/Command-comma cycles and additional menu jumps reused one window identity. The yellow button physically minimized it (`settingsMiniaturized=true`); actual Settings menu restored it. At620×440, native scrolling and pointer Diagnostics worked with readable content (`minimum-size.png`). Closing Settings left body/menu/runtime alive. |
| 3 — truthful controls | CUA read every section's distinct unavailable explanation. General Show Companion was actually pressed after developer hide and restored the existing panel without engine/session changes. Models has no editable provider/key form; Voice remains disconnected; Pets names only Wisp starter; inventory sections do not claim queried emptiness; Memory has no home/editor; Permissions cannot grant execution; Proactivity has no trigger. Ready/unavailable screenshots demonstrate safe fixed lifecycle descriptions. |
| 4 — focus/body/Spaces | Explicit management menu opens produced key/main Settings and a non-key/non-main body. Initial launch and status-menu open/dismiss retained independent target typing. After Settings/recreation, actual global pointer clicks reached all four transparent corners and the interior gap (5 clicks), scroll reached240, and fixed text reached the independent field. Opaque drag moved the body by100/-100 Quartz points. In the independent app's real full-screen Space, Wisp remained visible, a sixth gap click reached the target, scrolling reached480 and typing continued. Escape restored the ordinary Space. All observed body key/main facts remained false. |
| 5 — same runtime/faults/shutdown | Final normal smoke turn1 preceded menu/settings/recreation; distinct turn2 followed them with identical controller, bridge, runtime and Harness session. Separate bridge-loss and engine-failure runs injected SIGKILL only into the corresponding owned process while Diagnostics was open. Settings displayed Unavailable, stayed navigable, and actual status Quit worked; no second ready/restart occurred. Missing-config created no bridge/runtime and still supported Settings/Diagnostics/Quit. Repeat launch demonstrated another clean actual menu Quit. |
| 6 — regression/resources/reproduction | All native/bridge/live regressions passed. Current native `NSApp.windows.count` was2 before lazy Settings,3 settled afterward, and4 while a transient popup existed; it did not accumulate across ten cycles. One retained Settings identity, one body window/view, two owner observers, two signal sources, one status item, and0 hidden/1 visible body timer were observed. One bridge/runtime/session persisted across normal stress. Resource fields are observations of actual AppKit/current owned resources, not a fake allocation test. Exact-copy review can use the documented interactive runner and authorized global probe. |

The popup geometry comes from owned native status-frame facts plus public CG window observations. CUA application-menu actions are documented separately and are not misrepresented as status-menu clicks. On this host AX clicking the minimize button did not actually minimize; the successful physical pointer result is the one claimed. The initial preliminary run is excluded from final same-binary proof. The5 recorded screenshots contain only Wisp Settings.

## Limits and handback

The full-screen observation covers the independent test application's Space on this one display; it is not a multi-display hardware claim. The status popup has no CUA screenshot/AX text capture, as explained above; independent review should visually verify its disabled labels. No voice, durable home/memory, model selection, grants, alternate body, plugin/connection/skill management or proactivity feature is claimed. A passing interactive runner does not replace real operator observation. No protocol/archive or accepted01/02 evidence was edited, and no deployment/publishing occurred.
