# 07 voice implementation evidence — WIP, not accepted

## Current checkpoint — 054

The amended07 implementation is a work-in-progress checkpoint, **not independently accepted or shipped**. The participant cannot perform more live tests now. Remaining physical tests are explicitly pending; typed seams or generated audio do not replace them. Current approved contract is `0ce034eec8a32fa86ef7908d9b7021650c589331eb7edd9ef4b96af4e161d90e`. Build10 executable SHA256 is `9c15fe1696558846694855359d48a218228c8b7a9d31941ee9d4f50c6fe156c4`; later `.gitignore`/evidence edits do not change its build inputs. Coordinator owns final candidate identity, provider restoration, teardown, review and any WIP push.

- The user enabled Dictation after048. Subsequent real human microphone input produced local Harness replies and audible output confirmed by the participant. Old TCC/Dictation failures below remain historical evidence, not current blockers.
- Build08's four-key interruption attempt received negative listener feedback (“It kept speaking”). Build09 simplified the shortcut to Option–Space and added safe input diagnostics. Two actual shortcut activations while speaking produced releasing→idle; participant said “the shortcut worked.” Preserve this distinction rather than treating the original failure as a pass.
- Approved051 amendment/052 implementation admits the saved attached local or DeepSeek reasoning provider independently from on-device recognition and installed speech. UI explains cloud text transmission, unavailable recognition locales, installed output quality/novelty and language mismatch. Existing settings remain preserved; ordinary compatible defaults apply only to fresh configuration. Native suite,32JS tests, strict bridge rejection/duplicate cases and static signed-bundle checks passed. The duplicate test admitted at mostone local prompt; these checks made zero cloud calls. No downloads or credential-system changes.
- Build10 user-operated physical speech produced **three DeepSeek v4-flash turns** in generation78e53547-d4dd-49cf-a906-1025c9554b7b. All correlate recognized final→matching committed response/settledfalse→speaking. Receipts: eb79dda4-3da0-4489-8505-614059631e21 (turn1),25d5e4b9-4928-4d5d-a76c-7cf4579ad1b4 (turn2),718db113-4945-4ee8-8538-278f401fc4c0 (turn3). Participant confirmed replies audible and “DeepSeek working and shortcut also working.” This is run-level listener evidence; do not invent exact spoken content or per-turn subjective ratings. Empty/cancelled captures remained no-turn outcomes. Root/Builder did not initiate these cloud turns; coordinator records user-operated calls separately from agent test allowances.
- The participant still described Samantha as robotic. Audible functionality is proven in this run; satisfactory naturalness is not claimed. Existing installed quality labels are not a promise of natural speech. Arabic output voices do not establish Arabic on-device recognition; prior ar-SA probe was unsupported on this machine. No language model asset was downloaded.
- Root temporarily selected localqwen3:8b for remaining tests. Generation4a7bc410-f533-4b68-862b-f91839798288 completed one real recognized turn normally, not a thinking cancellation. Direct/plugin fixture ledgers were both0.
- One explicitly identified supplemental source used macOSsay/Samantha through the existing physical speaker into the actual microphone. Task7f527865-e53b-47ee-8357-26c67f280207 captured19846ms/198buffers/950400frames with nonzero samples but finalized empty text; zero model admission. This is a **failed controlled-source attempt**, not a successful recognition/cancellation test. No PCM/file injection, routing change or retry was substituted.

Final054 teardown: root restored the saved DeepSeek v4-flash selection without entering a key or running Test Connection. Attachment reported “Keychain access failed or was denied”; the saved selection is restored, but active cloud readiness is **not** established. This does not prove an invalid API key. No protected prompt retry or bypass was attempted. Root then quit; owned native/bridge/runtime PIDs were absent and launcher exited0. Resume through the ordinary user-facing Keychain approval flow if requested, then verify attachment before explicit speech. No automatic inference retry.

Remaining acceptance gates: shortcut start/finish from another owned focused app, shortcut while muted/no capture, cancellation during actual reasoning, recognized harmless-tool withhold/Deny/Cancel/AllowOnce exact effects, pending recognized approval Apply/Quit and stale teardown, shortcut conflict/nativeWake/recovery, and relevant final body/settings/memory continuity/no-orphan checks. Retain earlier deterministic/typed/06 proofs as supplemental. Independent implementation review and07 acceptance remain pending; an advisory WIP review must not be labeled acceptance.

Safe compact facts: [07-checkpoint-results.json](07-checkpoint-results.json). External detailed artifacts remain under work/wisp-07/07-build-052,07-verify-053,07-verify-054 and work/loop-state; they are not copied as transcripts or raw logs into the repository.

## Historical implementation and diagnostics

Dispatch07-build-047; accepted contract5acb61a95ede264984500c537a4f5be5f80169b017bcedaa9a6782270a341002. This is Builder evidence, not independent acceptance. External proof root: `../../work/wisp-07/07-build-047`; durable progress: `../../work/loop-state/07-build-047-progress.md`.

- Native state: failing-first `native-red.log`, then passing state assertions; later `native-05.log`, `native-06.log`, `native-07.log` include VoiceStore persistence/schema/symlink sentinel, generation and receipt frame validation, activation/repeat/final-only behavior, fake-provider capture/speech cancellation and completion races, mute, stale callbacks, unsupported/denied/no-speech seams, and pending approval speech suppression. The Native04 failure was a test expectation mismatch: an unsafe symlink correctly raised StoreError.unavailable rather than StoreError.unsafe; Native05 accepts either refusal and asserts the sentinel unchanged.
- Protocol: `voice-red.log` records missing-module red; `voice-01.log` passed seven scoped tests. `js-02.log` passes all31 JS unit tests, including strict UTF-8 split at every byte boundary, invalid/oversized frames and committed-only response filtering. No live audio in these checks.
- Actual local reasoning: `voice-live-01.log`/`voice-live-01/events.json` record two successive real qwen3:8b/Harness completed turns with matching message receipt and turn IDs, then a third active-turn cancellation with no reply. Matching completed cancellation is harmless; shutdown is clean with no orphan. One attempt per scenario. This test is explicitly a typed-text transport seam and does not claim microphone or audible output.
- Boundary rejection: `voice-security-01.log` records actual bridge startups receiving foreign generation, extra arbitrary session ID or malformed UTF-8. Every case fails before voice processing, leaves no user session, performs zero model calls, and shuts down cleanly.
- Permission lifecycle regression: `permissions-lifecycle-01.log` records cancellation, EOF, signal, malformed decision and stale generation with two matching pending approvals per case, zero effects, every closure drained, no false completion after failure and clean cleanup. These deterministic registry fixtures make zero model calls. Prior06 accepted direct/plugin/child and action-authority evidence is retained; voice admits no new capability.
- Duplicate submission: `voice-duplicate-01.log` rejects the second identical live request, observes exactly one processing admission, emits no speech result, and cleans up. This attempted at most one additional local prompt; there is no second retry or cloud call.
- Builds01–03 compile on the installed SDK without launching Wisp. Build04 compiled successfully and is the hardware-gate candidate and includes the pre-start synthesis cancellation fix. Native checks run isolated sentinel Keychain/storage fixtures only; no real key or cloud access.
- Actual noncapturing UI: coordinator-only observations at `../../work/loop-state/07-build-047-root-ui.md`: build03 Voice settings displays en-US on-device available, selected installed speech voice/rate, registered shortcut, actual Wisp microphone allowed/speech not requested. Mute/unmute creates only idle/muted states, zero utterances. Voice change leaves qwen3:8b and companion identity unchanged. Quit exits0 and all three owned processes are absent. Build04 restart also retains saved Eddy en-US/rate0.5, en-US recognition and mute, the same companion/home/memory and qwen3:8b. Actual Wisp speech permission remains not requested; no capture/playback occurred. Root verified clean exit and all three build04 PIDs absent. This is input-actuator evidence, not independent review.

## Still required

Actual global shortcut from another owned window and conflict handling; explicitly authorized physical microphone source and Wisp speech consent; two recognized local-model/audible-TTS rounds; real capture/reasoning/speech interruption and recovery; recognized harmless tool with native withhold/deny/cancel/allow 0/0/0/1 effects; touched body/Settings/memory/lifecycle continuity and final identity/cleanup. No typed text, direct PCM or generated file substitutes for the physical input and listener gate. No microphone, recognition task, speech playback, cloud call or download has been executed by Builder.

## Live resume048 — packaging repaired, OS speech-service boundary unresolved

The participant explicitly authorized the bounded physical phrase/listener test. Root remained the sole desktop actuator; Builder never activated capture or played audio. External proof: `../../work/wisp-07/07-resume-048`; root observations: `../../work/loop-state/07-resume-048-root-ui.md`.

- Frozen04 actual Wake caused a TCC SIGABRT with missing speech usage description. Both strings existed, but the binary's SwiftPM signature did not bind Info.plist/resources and the crash attributed responsibility to the host app. `bundle-red.log` reproduces the static packaging defect. Scoped repair signs the completed Wisp bundle ad-hoc and launches through LaunchServices. No privacy reset, host bundle modification, signing credentials or network is involved.
- The first LaunchServices developer attempt failed before app start with EPERM while launchd opened log paths in protected Documents. Launcher now precreates0600 streams/FIFO in its own0700 temporary directory, then copies proof back after exit. Build05 launched with PPID1 and the actual Wisp microphone/speech consent flow succeeded; `bundle-green.log` verifies the corrected bundle. This resolves the packaging boundary, not speech recognition itself.
- Two build05 tasks reached listening then unavailable, with “No usable speech was recognized,” and no model request. Both belong to local qwen generation643f0f59…f9a8/runtime12466. The participant said they were speaking and heard no reply; that statement cannot be assigned to one exact utterance from the retained timing alone. Later the participant independently attached DeepSeekflash generation84c1d18b…1ec2/runtime12517 and returned to local12b443f3…7928/runtime12549. There are no testing/connection-test/voice-processing/result events across the run. Builder inspected no credentials and initiated no cloud inference. Safe per-line attribution is `live05-safe-events.json`; do not describe the intervening attachment as local.
- One authorized diagnostic build07 attempt localized the failure: utterancefd6214cb…4fa8, local engine8f087384…b347/runtime12852, task successfalse, no final, **kLSRErrorDomain201,4ms, inputBuffers0/inputFrames0**. No samples or transcript were retained and no model request occurred. Installed SFSpeechRecognitionTask.h:94 and Apple's task-error documentation identify201 as Siri or Dictation disabled. This does not prove a muted microphone or absent human speech. Further blind capture attempts were stopped.
- Native08/09 tests pass categorical diagnostics, transcript exclusion and aggregate-only input accounting. Native10 adds the specific201/102 friendly-status regression; build08 is the final prepared bundle with corrected readiness wording. Static signature checks remain required. Root actual build05/build07 Quit reports clean/noOrphan, native bridgeExit0 and exact owned PIDs absent. No Builder app or helper remains.

**Historical048 gate (later resolved by the user):** the system speech service must be enabled through a separately explicit user choice before another bounded physical test. Current capture authorization and spoken-source/listener participation are already supplied; do not ask for those again merely because of this diagnosis. No OS Dictation/Siri setting, asset download, audio routing or privacy database was changed by Builder. After the service boundary is resolved, the original two real roundtrips/audibility, phase interruption, shortcut, recognized native approvals and lifecycle criteria still require proof and independent review. Slice07 remains incomplete; counters/acceptance are coordinator-owned.

Root's subsequent read-only System Settings → Keyboard inspection confirms Dictation is OFF. The pane lists Arabic (Saudi Arabia), English (Saudi Arabia), and Automatic/MateView GT input. Root changed none of those settings. This independently observed OS state supports the201 diagnosis; it does not establish that the selected Wisp en-US speech assets will be ready after a change. Any later asset failure must be handled as its own concrete resource boundary.
