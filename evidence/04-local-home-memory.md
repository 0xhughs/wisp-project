# Slice 04 — local home and durable memory evidence

Latest candidate includes repair `04-repair-028`, pending independent review. Review027 independently confirmed both026 security fixes, then rejected a separate live-home-move recovery gap. The original `04-build-024` candidate was rejected for two deterministic storage gaps; its historical evidence below must not be read as approval. See the repair comparisons at the end and `04-repair-028-results.json` for the current executable/source hashes and directly repeated checks.

Builder dispatch `04-build-024`, contract `e6ee10ff30ab32fdc8298ac6abb357dcf89eb0c49c653753ba230917efac41fa`. Independent implementation review remains pending. Machine outcomes are in `04-sanitized-results.json`; private fixtures, audit logs and operator receipts are outside the checkout in `work/wisp-04/04-build-024` relative to the enclosing workspace. No real credential was used as test data.

## Result and applicability

A real native chooser selected a dedicated home, four categories were edited using native controls, and the same persistent companion UUID survived menu Quit and automatic bookmark relaunch. A fixed developer recall request containing no answer returned the saved unique fact through native → bridge → pinned Harness → authorized local Ollama. Actual durable `request/header` records prove the deleted fact absent from the next model input; a later restored document was present and recalled exactly. Both recall turns had zero effects. Each launch has a new internal session; this is durable knowledge and identity, not resumed transcript history.

Final executable SHA-256 is recorded in the JSON. The final source change was one failure transition: a memory-load error before bridge start now sets Engine Unavailable. Actual unavailable-folder GUI exposed Starting previously; the repaired binary displayed Unavailable, accepted native same-home reselection after an owned fixture move, then attached the real engine. The final binary also passed positive and negative model-input/recall proofs and the full security runner. Full body and shell regressions immediately preceded this one-line error-path repair; body, focus/editor, shell, and bridge/context source did not change afterward. Thus those tests apply to their unchanged paths; they are not mislabeled as runs of the final executable. Subsequent changes only expanded assertions and evidence.

Tested: macOS 26.5.2 (25F84), Apple Silicon, Swift 6.3.3 command-line SDK and local filesystem. No full Xcode/XCTest, new dependency, entitlement, permission bypass or model installation. Network/remote filesystem behavior and final distribution are not claimed.

## Repeatable commands

Run from the repository. Set `WISP_CHECK` to a fresh absolute external scratch directory, `WISP_RUNTIME` to the existing verified built runtime, and `WISP_ROUTE` to the authorized route file. Do not use the real application-support location. The native executable requires `--test-support` for developer launches; live runners supply a direct child of their marked scratch directory.

```sh
desktop/scripts/build-macos.sh --scratch "$WISP_CHECK/build"
desktop/scripts/test-native.sh "$WISP_CHECK/native"
node --test desktop/tests/body-bridge.test.mjs desktop/tests/memory-context.test.mjs spike/tests/client.test.mjs
/usr/local/bin/node --import "$WISP_RUNTIME/upstream/node_modules/.pnpm/tsx@4.22.4/node_modules/tsx/dist/esm/index.mjs" desktop/tests/memory-engine.mjs "$WISP_RUNTIME/upstream"
swiftc desktop/macos/Sources/WispBody/MemoryStore.swift desktop/macos/Sources/WispBody/HomeStore.swift desktop/tests/HomeFixture.swift -o "$WISP_CHECK/HomeFixture"
node desktop/tests/memory-security.mjs --runtime-root "$WISP_RUNTIME" --route-file "$WISP_ROUTE" --scratch "$WISP_CHECK/security" --app "$WISP_CHECK/build/Wisp.app/Contents/MacOS/WispBody" --fixture-helper "$WISP_CHECK/HomeFixture"
node desktop/tests/memory-live.mjs --runtime-root "$WISP_RUNTIME" --route-file "$WISP_ROUTE" --scratch "$WISP_CHECK/gui" --app "$WISP_CHECK/build/Wisp.app/Contents/MacOS/WispBody"
```

The last command is interactive and must run with persistent stdin. Its printed instructions require real chooser/editor/menu actions. Operators `recall` and `smoke` only trigger fixed developer probes, not arbitrary text. Quit using the actual menu, rerun with the same scratch to exercise retained-home resolution, and inspect the private session audit via the unchanged `spike/client.mjs` `readDurableSession`. Assert user/message contains no answer, request/header contains the expected snapshot, companion ID equality and session inequality. Deletion must be checked against the actual header, not just a stochastic model response.

For the existing live regressions, use `desktop/tests/body-live.mjs` and `desktop/tests/shell-live.mjs` with the same runtime/route/app arguments and `--fixture-helper "$WISP_CHECK/HomeFixture"`. Body runs all seven cases when `--case` is omitted; shell takes `--case normal`, `bridge-loss`, `engine-failure`, `missing-config`, or `repeat`, each with a fresh scratch. See the runners and management-shell guide for interactive operator checkpoints. Generated HomeFixture is a test-only executable, never bundled into Wisp.

## Criteria and observed proof

1. **Retained folder and recovery.** Actual first chooser cancellation left only the private lock, with no retained pointer or initialized home. Actual chooser selection initialized the dedicated folder. Later runs supplied no home-path argument and used no chooser; UUID and exact saved revision loaded from the retained bookmark. Final unavailable-home run had no engine and useful Locate same folder UI. After restoring access and moving that owned folder, native chooser reselection recovered the same UUID and three entries. No new identity was created. Final subsequent automatic relaunch followed the moved home. Private sequence: GUI runs ending `9775521` (recovery/negative input), `9905317` (automatic restart/positive input); `memory-oracles.json` records safe outcomes.
2. **Memory management.** Actual Add/Save for each of preference, project, instruction and fact; Edit draft survived General navigation and close/reopen. Cancel restored committed text. Delete confirmation identified category and exact entry; Cancel kept it and confirmed Delete removed it. A real external edit while a draft was open caused Save conflict and retained the draft. Cancel/Reload exposed the external version; native clipboard paste/Save preserved Unicode, newlines and literal template braces. CUA keyboard synthesis alone cannot reliably enter Unicode, so native paste was used. Corrupt JSON was visibly refused and kept byte-for-byte until the test operator restored it. Final long-entry scroll moved its scrollbar from 1 to 0. Actual Tab moved text→Save→Cancel; Shift-Tab returned to Save; Space saved. All eleven management destinations remained navigable. Final recovery run showed Saved/attached, then valid external Reload showed Saved/next app launch without creating another engine.
3. **Actual knowledge and identity.** Positive initial restart proof and final positive restart returned the unique saved fact. Final actual headers separately proved absence after deletion and presence after restoration, plus literal template preservation. Fixed actual user/message records contained no answer. Both turns had zero effect ledger entries. Body recreation and shell smoke checks retained current controller/bridge/session; app restart retained companion UUID while creating new runtime/session. Saved changes are immutable for an attached engine and take effect next launch.
4. **Persistence and loss prevention.** Real temporary-directory native assertions cover schema/UTF-8/category/unknown-version/duplicate/count/size refusals, create/save/restart, owner modes, symlink/hardlink managed files, private-state and managed-directory symlinks, replaced marker/ancestor, outside canary, moved home, read-only directory, pointer-last interrupted initialization, and old-or-new valid publication. Injected before/after publication failures supplement real filesystem refusals. An actual NSFileCoordinator writer overlaps Wisp Save: Wisp waits, then rejects its stale revision, retaining the writer's document. A noncooperating change injected before publication also causes conflict. `native-final3.log` is the expanded final assertion result. This is not protection against a malicious same-user process racing the final rename; that documented limit remains.
5. **Security and ownership.** Final `security-final/result.json` has 12 passing gates: real two app processes sharing private state, distinct private states sharing the same home, lock release on owner death and safe identity/memory recovery, staged-snapshot removal, `.env`/credential-like canary absent from actual model headers/environment/native diagnostics, corrupt bytes preserved with Unavailable/no engine, inaccessible home refused, adversarial standing instruction causes no effect, and no automatic raw failure dump. The harness cwd/DSH_HOME stay private outside editable home. `files/` is storage only; no crawl. Existing accepted approval adapter and capability restrictions remain unchanged.
6. **Body/shell regressions.** `body-final/live.json` records all seven real cases passing. Five actual shell scenarios passed their original ownership/cleanup oracles; normal included two distinct real turns in the same session, all eleven destinations, one retained Settings window, ten close/reopen cycles, minimize/restore, hide/show and body replacement. After Memory editing, actual bounded CGEvent input to the verified owned ClickTarget doubled its fixed typed text, transparent corner/gap/edge clicks incremented its counter three times, and scroll moved 0→240. Opaque body drag moved x by 100 while the target stayed key and body remained non-key. Real menu Quit cleaned owned processes. Bridge loss/engine failure left ordinary Settings usable and Diagnostics unavailable. Counts of all AppKit windows may include hidden framework menus/panels; the proof asserts one Settings/body/status owner, not a false constant count of framework windows.

Owned-window screenshots were captured through CUA in the Builder conversation: initial Memory; final normal Models unavailable boundary; final recovered General showing attached/ready; final Memory showing Unicode content and next-launch status. These inline private GUI observations include owned test text/path and are deliberately not copied into sanitized covered evidence. No screenshot of unrelated applications was retained. Safe GUI facts are summarized in the JSON; raw events remain external.

## Isolation incident and limits

After a completed test Quit, a CUA observation automatically relaunched the exact current bundle without test arguments. The verified accidental process was stopped. Its only default-support effects were a newly created empty Wisp directory and zero-byte lock, whose birth times matched that launch. After verifying type, owner, link count and exact contents, only those conclusively new artifacts were removed; no preexisting user data was removed. The private incident receipt records command/time and checks. Subsequent Quit actions used runner/process observations only, never CUA access to a stopped app. This is a disclosed isolation incident, not a claim that default support was never touched.

No hot reload, automatic learning, credentials UI, voice, plugin activation, arbitrary prompt endpoint, multiidentity switching or deployment is delivered. The private test trace may contain memory/model data; covered evidence retains only bounded safe outcomes. User-entered secrets are not detectable with a guarantee; the product explicitly labels memory as nonsecret knowledge.


## Repair04-repair-026 — before/after comparison

Independent review025 identified two concrete failures. Both were reproduced against unchanged original source in external repair scratch before editing. The original LockProbe reported two live READY owners and two SAVE_OK results after unlinking the home lock. RecoveryProbe reported unsafe README symlink recovery accepted with pointer=true, then ordinary read refused. Those are material gaps in the prior12-gate security suite, not dismissed races.

**P1 repair:** directory-inode flock provides stable exclusion despite named-file replacement; named-file ownership is also verified before read/load/choose/stage/save and immediately before memory publication. Ten real two-process cases cover home/private unlink, replacement regular file, symlink, hardlink and unsafe mode. Rivals were refused, each old-owner operation refused, memory and outside canary stayed unchanged, and a fresh owner recovered after process exit and correction of the test-owned invalid lock. The exact new test compiled against the original source fails on home/unlink with second owner admitted. Four additional actual app/Harness cases (home/private × unlink/replace) prove no rival bridge/engine while the owner lives, followed by same-ID/revision recovery on owner exit. The running owner remains the single engine; no continuous filesystem monitor is claimed.

**P2 repair:** initial and interrupted choose use the ordinary full snapshot validator before pointer publication, including README. Symlink, hardlink, wrong mode, directory and FIFO variants all refuse without a pointer, leave the canary unchanged, and retry successfully with the original pending UUID after the fixture is corrected. Nonblocking no-follow opens prevent FIFO validation from blocking. The original independent RecoveryProbe now reports RECOVERY_REFUSED. Actual native chooser selection of the unsafe pending folder displayed unavailable, kept pointer absent and started no bridge. After the operator repaired only the generated fixture's README, another real chooser selection committed the same marker identity, attached the real Harness and completed a harmless model smoke with zero effects. Actual application-menu Quit cleaned the process group. An owned-window screenshot of the refusal is recorded inline in the Builder conversation.

Repeatable regression additions (same external variables as above):

```sh
swiftc desktop/macos/Sources/WispBody/MemoryStore.swift desktop/macos/Sources/WispBody/HomeStore.swift desktop/tests/HomeOwnershipProbe.swift -o "$WISP_CHECK/HomeOwnershipProbe"
python3 desktop/tests/home-ownership.py "$WISP_CHECK/ownership" "$WISP_CHECK/HomeOwnershipProbe"
desktop/scripts/test-native.sh "$WISP_CHECK/native"
```

Rebuild Wisp and HomeFixture using the commands above, then run `memory-security.mjs` in fresh scratch: now14 passing gates, including actual engine ownership under lock replacement. All15 JS tests remain green. Private repair scratch `work/wisp-04/04-repair-026` contains `lock-red.log`, `recovery-red.log`, `ownership-red.log`, `ownership.log`, `ownership/result.json`, `native-final.log`, `recovery-green.log`, `build.log`, `security.log`, `security/result.json`, `gui-refusal.json`, `gui-proof.json`, and the actual GUI run ending1503435. The independent minimal probe sources remain available with review025; they are supplemental to the committed repeatable regression suite.

Only HomeStore product source changed. The repair executable's exact hash is in the new JSON. It directly passed storage/ownership/native, actual engine security, unsafe chooser recovery and real smoke. Unchanged editor/focus/body/bridge/system-prompt paths retain prior source-applicable observations. This repair did not rerun full five-case shell GUI, every menu item, long-entry scrolling, moved-home GUI or the full positive/deleted-fact recall sequence; no claim of new execution is made for those. Reviewer025 independently reproduced substantial unaffected GUI/body/recall coverage and explicitly recorded its remaining coverage limits.

No default-support data or unowned resource was touched during this repair. No CUA observation followed Quit. No active owned apps, probes or engines remained at handback. Earlier024 CUA relaunch incident remains disclosed above. Contract unchanged; no self-approval, later-slice feature or distribution claim.


## Repair04-repair-028 — live same-home recovery

**Material comparison:** review027 confirmed both026 findings resolved, but its live-store MoveProbe and real GUI showed that moving an open home caused Reload refusal followed by a permanently refused Choose action. The same original probe was compiled against input source and reproduced `STALE_READ_REFUSED; RECOVERY_REFUSED` before this edit (`move-red.log`). Recompiled on the repair it reports `STALE_READ_REFUSED; RECOVERY_OK sameIdentity=true` (`move-green.log`).

The explicit chooser path now validates the selected directory against the already-held directory device/inode and named lock descriptor, then validates marker/UUID and all managed content. It persists the updated bookmark and rebinds the path while retaining both original lock descriptors. It never temporarily releases/reacquires ownership or creates another controller/engine. Ordinary operations still reject a stale path until explicit recovery. A copied directory with the same marker, wrong identity, modified same-inode marker, malformed memory or replaced named lock cannot use recovery to bypass026 protections.

The expanded native assertions keep HomeStore alive through rename, verify stale read refusal, wrong marker/invalid content refusal, successful same-snapshot recovery, then save/load/stage at the new path. Moving again and replacing the lock still refuses. All earlier native schema/fault/README/security assertions pass. `home-ownership.py` now runs the prior10 real two-process mutation cases plus a live-move scenario with20 reselections under an independently opened continuously contending directory descriptor:32 lock attempts, zero acquisitions. Rival HomeStores are refused before and after rebind; copied inode/wrong identity refused; owner-exit recovery succeeds. Source retains the same descriptors throughout—the observed contention check supplements that invariant rather than claiming exhaustive scheduler/race immunity.

**Actual final-binary GUI and runtime:** in a marked isolated home, Wisp attached an empty snapshot. Native Memory saved a unique nonsecret fact. While that same app/bridge/runtime stayed alive, the operator moved only the owned home, clicked Reload (visible unavailable/disabled editing), then used the real native chooser to select the moved directory. Memory became usable at the moved path and showed next-launch status. An actual Edit/Save after reconnect succeeded. Exactly one ready event, the same controller/runtime/session and unchanged attached revision were recorded throughout. A separate real rival app with another support binding to the moved home was refused busy and started no engine.

The fixed recall after reconnect returned unknown; actual durable request/header lacked the newly saved fact, proving the current-launch snapshot did not hot-reload. Actual menu Quit cleaned the engine. Ordinary relaunch with the same support and no chooser/home-path injection loaded the same UUID and latest saved revision in a fresh session; its fixed recall returned the unique fact exactly. Actual request/header now contains it; both actual user/message records contain no answer. Both turns had zero effects, and both menu Quits cleaned ownership. An owned-window screenshot of successful moved-path recovery and pending-next-launch text was captured inline through CUA.

**Reproduction/evidence:** use the same build, native, HomeOwnershipProbe/home-ownership.py, memory-security and memory-live commands above with fresh external028 scratch. Native complete log is `work/wisp-04/04-repair-028/native-complete.log`; process cases `ownership/result.json`; rebuilt app `build/Wisp.app`;14/14 actual security gates `security/result.json`;15 JS tests `js.log`. Original independent MoveProbe source is in review027 scratch. GUI runs ending3156752 and3379970 retain private events/clean-exit receipts. Private `move-rival.mjs`/`move-rival.json` reproduce the actual rival-app boundary; `audit-move.mjs`/`move-gui-oracles.json` derive16 safe actual frame/request-header equality/input/recall outcomes. Covered `04-repair-028-results.json` retains sanitized results and final executable/HomeStore hashes, not fixture text or paths.

Only HomeStore product source changed. Exact028 executable directly passed targeted GUI/rebind/model/restart/engine-security checks; native/test changes do not change that build. Full five-case shell/menu/long-scroll checks were independently completed on027 and remain historical source-applicable observations for unchanged shell/editor/body code; they are not mislabeled as rerun on028. No contract weakening, copy/merge feature, new dependency, hot reload, credentials change or later-slice work. All028 owned processes stopped normally; no CUA observation after Quit and no default-support effects. Fresh independent review remains required.
