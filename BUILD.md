# BUILD — active contract

Slice: 11 Connections and custom MCP
Archive: (none; not shipped)

## Goal

Configure external capabilities through friendly Wisp Connections or advanced MCP configuration, on the same Wisp, through the pinned DeepSeek Harness MCP client and the accepted 06 permission gate.

Ordinary users manage a closed Connections catalog. Advanced users may inspect and edit accepted custom-MCP fields for the same compatible class. Wisp owns the Settings surface, protected credentials, and permission classification. Harness supplies the capability system (`@deepseek-ai/dsh-mcp-client` at pin `d347e703908d0406b7a7ef80e3a0e594d86b2215`). Compatibility and safety govern availability: unsupported classes stay unavailable.

The contracted demonstration is one Wisp-authored local **stdio** MCP fixture with one harmless ledger-append tool. GitHub, Google Drive, Notion, Calendar and Slack are examples in product copy, not a shipping bundle. Parked 10 is unaccepted: plugin-delivered Connections remain unavailable; this slice does not assume 10 mounted MCP and does not enable the 10 catalog class `mcp-connection-plugin`. Slice 07 is an unaccepted shared lifecycle: record retest after any Apply/lifecycle/bridge change; do not treat the voice loop as working.

## Done when

1. **Wisp-owned Connections surface; ordinary vs advanced; honest inventory.** Settings → Connections is a working management section, not the 03 placeholder that has not queried inventory. It lists a closed, Wisp-authored catalog: one installable demonstration Connection (local stdio MCP fixture); an advanced custom-MCP editor for that same compatible class (accepted mcp-client fields, Wisp-fixed safety values); and explicit unavailable example rows (GitHub, Google Drive, Notion, Calendar, Slack, remote/non-loopback HTTP, `npx`/npm/git MCP servers, MCP resources/prompts, plugin-delivered MCP). Each row shows a truthful status among at least: not installed, saved, applying, active, incompatible, unavailable. Unsupported rows cannot be enabled. No empty-list implication that those named services were queried. Keyboard-accessible Enable/Save, Advanced configure, Remove/Disable and Apply exist only for the supported demonstration class. Cancel/revert leaves the saved composition unchanged. No composer, transcript, Harness web/chat UI, or `dsh` CLI is exposed. Plugins remains the 10 catalog and still cannot enable `mcp-connection-plugin`.

2. **Setup, status and removal use overlay-and-restart; one compatible MCP server.** Enabling the demonstration persists a versioned Wisp connection snapshot, then reuses the existing owned engine replacement path (stop old bridge/generation, start a new process with the composed overlay). The running product overlay inserts one Wisp-owned covered adapter that drives the inspected pin `apply` / `startConnection` with accepted Config fields only: `transport: 'stdio'`, `serverName` matching `^[A-Za-z0-9_-]{1,32}$` (product default `wispdemo`), `command`/`args` bound to the same Node used to launch the engine plus the covered fixture path (no shell interpolation, no `npx`, no user-chosen executable), `cwd` a launch-owned directory, `failOnStartupError: true`, `reconnect.enabled: false`. After `initialize`, `wisp.inventory` reports connection id, serverName, public tool names, transport `stdio`, and a nonsecret config digest. Native status matches that snapshot. Disabling removes the insert, restarts, and `mcp__wispdemo__record` is absent. Failed start leaves an honest unavailable engine with the saved snapshot retained; Wisp does not silently keep the old process or auto-revert. Identity, selected home, durable memory, Models route, and the 10 plugin snapshot survive Connections Apply.

3. **06 MCP admission on a real local server and actual tools/call.** Normal-mode inventory, when the demonstration is actually loaded, includes exactly one new MCP tool in addition to whatever 06/09/10 already admit when those routes are loaded: public name `mcp__wispdemo__record` (identity `(serverName, rawName) = (wispdemo, record)` per pin `publicToolName`). Source id is the closed origin `wisp-mcp` (MCP route in the 06 table; not Direct, not a 10 plugin, not a skill, not stock). Wisp binds the **actual registered** ToolDefinition object and generation after `loader.await()` and before `seal()`. Native confirmation shows a trusted label, operation `append-test-record`, exact nonsecret destination (launch-owned ledger), material fields, and the public tool name. Controls remain Allow Once, Deny, and Cancel Request; initial focus Cancel Request; no Allow Always. A spoken or typed “yes”, saved key, connection save, plugin install, or memory instruction is not a grant. **Zero** MCP JSON-RPC `tools/call` (and zero fixture ledger effect) before approval and after deny, cancel, Escape/close, timeout, stale generation, or missing grant. One scoped Allow Once yields **one** actual upstream `tools/call` of raw name `record` (never the public name) and one exact ledger record. `consume()` on the admitted object runs immediately before that RPC. Developer fixtures and the 10 demonstration tool stay on their existing gates. Settings → Permissions lists the demonstration MCP tool as Ask-each-time when mounted, otherwise MCP unavailable; skills, Accessibility (15), visual click (16), Windows (17), and stock shell/fs/web stay unavailable.

4. **Unknown MCP actions fail closed; reconnect/list-change invalidates; credential/endpoint isolation.** MCP’s pinned bridge has no trustworthy semantic permission classifier (`packages/mcp/mcp-client/src/tools.ts` `createDefinition`). Wisp owns classification. Extra or unknown advertised tools fail initialize (`WISP_INVENTORY`) rather than becoming Allow-able. Sending, deleting, purchasing and privileged MCP tools are not admitted in this slice; they cannot execute. Resources and prompts remain unsupported (tool-only bridge). After `seal()`, `tools/change` (list-change or generation swap) keeps the existing 06 behavior: `invalid=true`, unused grants revoked, owning agents cancelled; subsequent MCP `tools/call` count stays zero. Product reconnect stays disabled; a test-only overlay may enable reconnect or emit `notifications/tools/list_changed` to prove invalidation. Credentials for optional connection secrets use Keychain (Mac) or an isolated protected test store: opaque ids in the snapshot, **never** secret values in `memory.json`, Diagnostics, overlay YAML, argv, or plugin notes. A secret bound to connection A does not appear in connection B’s child `config.env`, in a disabled overlay, or in diagnostics. Ambient pin scrubbing (`/KEY|PASSWORD|SECRET|TOKEN/i` and `DSH_*`) is not a substitute for that isolation. If a confirmation cannot be rendered without revealing a secret, the action is unavailable. No required online account.

5. **Product invariants and later-slice boundaries.** One companion UUID and Wisp home persist across Connections Apply/restart. Transparent-region click-through, body movement, Settings close/reopen, menu navigation and Quit remain. No chat window, prompt composer, conversation history or Harness UI. Models remains the sole owner of reasoning keys. 09 URL/file/time tools stay Wisp-owned Direct (`wisp-safe-action`), not MCP; stock `tool-bash` / `tool-fs` / `tool-web` stay disabled. Connections Apply cancels pending approvals and owned engine work through existing stop/restart generation rules. Slice 12 skills, 13 pets, 14 release, 15–17 computer control, 18 wake-word, 19 skins, 20 proactivity, and 21 remote are not implemented. Open decisions (18/20/21) stay unresolved. **Retest 07** after any edit to CompanionController Apply/lifecycle, EngineBridge, VoiceLifecycle, voice-result filtering, or permission allowlists. **Retest 10** overlay/inventory/Apply if 11 changes overlay composition, `classifyInsert`, `wisp.inventory`, or plugin snapshot attach. Do not treat 07, 08, 09, or 10 Done when as satisfied. Do not spend the reserved DeepSeek greeting.

6. **Observable proof with honest Linux/Mac split.** Linux-supplemental JS protocol, overlay, classifier, admission, and real-local-stdio MCP tests pass on this host. Native Swift sources may be written on Linux but remain **uncompiled/unverified** here. Actual macOS Settings/permission keyboard, NativeChecks compile, Keychain CRUD, live local Ollama MCP 0/0/0/1, audible confirmation, and 07 shortcut/mic/TTS retest are Mac/human backlog items, not Linux passes. Isolated pin-runtime initialize remains **missing-external-resource** on this host (pnpm 11.7.0 / prepared `.wisp-spike.json`) until that runtime exists; record the gap and still land Linux-supplemental tests. No reserved DeepSeek call, no model download, no key inspection, no merge/publish/spend. Candidate/contract identities and pristine pin/archive checks are ready for independent plan review then, after implementation, independent implementation review. This dispatch does not approve the plan or mark 07, 08, 09, or 10 Shipped.

## Out

- A required online account, OAuth/SaaS login, or unconfirmed external writes.
- Shipping GitHub, Google Drive, Notion, Calendar, Slack, or any named third-party connector bundle.
- Enabling stock `tool-bash`, `tool-pwsh`, `tool-fs`, `tool-fs-search`, `tool-web`, `web-fetch-http`, `web-search-deepseek`, jobs, workflows, goals, skills, general sub-agents, or treating 09 URL/file/time as MCP tools.
- Allow Always, spoken-yes grants, treating connection save / plugin install / saved key as Allow Once.
- MCP resources, MCP prompts, or inventing a semantic MCP permission classifier.
- Arbitrary user-chosen stdio executables, `npx`/`npm`/`git` MCP installs, remote (non-loopback) Streamable HTTP, or enabling 10 catalog `mcp-connection-plugin` as a silent third-party plugin mount.
- Chat composer, transcript UI, Harness web UI, `dsh` CLI as the Connections surface.
- Voice-loop acceptance (07), hardware onboarding acceptance (08), plugin-management acceptance (10), skills (12), pets (13), release (14), Accessibility (15), visual click (16), Windows (17), wake-word (18), proactivity behavior (20), remote doorways (21).
- Resolving SLICES Open decisions. Inventing numeric performance targets or GB/VRAM cutoffs.
- Spending the reserved DeepSeek agent greeting 0/1 or the exhausted 05 cloud 6/6; downloading models; inspecting keys; merging, publishing, signing, or deploying.
- Editing accepted `spike/`, slice archives 01–06, or writing `slices/07-*.md` / `slices/08-*.md` / `slices/09-*.md` / `slices/10-*.md` / `slices/11-*.md`.
- Inventing stock SDK MCP list/install/remove RPCs or calling Harness web settings controllers.
- Disabling permission, privacy, credential, cancellation or release safeguards to make tests pass.

## Constraints

**Source and pin.** Keep pristine Harness `d347e703908d0406b7a7ef80e3a0e594d86b2215` and full SDK/base. Do not switch to `sdk-minimal`. Do not edit upstream tracked files. Product adapter/overlay remain under `desktop/engine/`. Only a porcelain-clean clone at that revision may support implementation-time upstream claims. Inspected pin dispatch (`packages/sdk/server/src/server.ts` `handleRequest`) remains initialize / `session/prompt` / shutdown. MCP lifecycle is **not** a stock SDK method. Do not call Harness web settings controllers. Do not enable disabled overlay rows (`product.patch.yml` and `DISABLED_STOCK_IDS` already list `tool-bash`, `tool-fs`, `tool-web`, `web-fetch-http`, and related stock execution).

**Verified MCP seam (do not invent APIs).** Inspected pin facts:

- `packages/mcp/mcp-client/README.md`: tool-only bridge; resources/prompts unsupported; public names `mcp__<serverName>__<rawName>`; accepted fields are the config catalog (`transport` `stdio` | `streamable-http`, `serverName`, stdio `command`/`args`/`env`/`cwd`, HTTP `url`/`headers`, `toolCallTimeoutMs`, `failOnStartupError`, `reconnect.*`). Base bundle does not mount servers. Nothing ships enabled.
- `packages/mcp/mcp-client/src/index.ts` `apply` / `startConnection`: one server per instance; `inject: ['tools']` only; `failOnStartupError` default **false**; duplicate `serverName` fails load; `apply` is async and awaits initial connection + `tools/list`.
- `src/tools.ts` `syncTools` / `createDefinition` / `callToolUncached`: full generation or none; `tools/call` sends the **raw** name; abort signal forwarded; definition has no semantic permission classifier; `notifications/tools/list_changed` re-syncs.
- `src/transport.ts`: stdio spawn with `scrubbedParentEnv()` then merge `config.env`; Streamable HTTP uses `url` + `headers`.
- `src/connection.ts`: reconnect default enabled; exhausted attempts unregister tools; with reconnect disabled, last tools may stay listed while calls fail. Wisp product overlay sets `reconnect.enabled: false` and remounts via Apply/restart (SDK `patchReload: startup`, HMR disabled), matching 10.
- Startup/discovery timeouts are the MCP SDK 60s default; an unresponsive server can delay initialize/teardown. `failOnStartupError: true` so a failed initial sync rejects activation rather than starting with ghost tools.
- Image/audio/embedded-resource MCP results and task-required tools are not this slice’s demonstration. The fixture is text/ledger only.

Therefore slice 11 wraps **Wisp-owned overlay insert of a covered product adapter** that calls the inspected pin `apply`/`startConnection` with a resolved Config. It does not claim `dsh plugin add`, live HMR, loader hot-create, or a marketplace of MCP servers.

**10 obligation (unaccepted; do not assume).** Parked 10 candidate `f8b24da610370c9575e04ef1d2d9dff54c9d552951812eb60df06b2029489fb8` is IMPLEMENTATION_READY, not Shipped. Plugin-delivered Connections remain unavailable. Do not enable catalog id `mcp-connection-plugin`. Do not insert unknown third-party plugin paths to “get MCP.” Current `classifyInsert` rejects `@` package specs and unknown ids; 10 `composeOverlay` requires `extraInserts` empty. 11 may extend overlay composition with a **new closed MCP insert class** (covered product file + inject `['tools','wispPermissions']`), orthogonal to the 10 demonstration plugin. Default (connection disabled) must keep 10 overlay tests green: no demo plugin tool unless enabled; no MCP public names; hostile extra unadmitted tools still fail initialize. If overlay/inventory/Apply/startAttachment change, record a 10 overlay/inventory retest. Update the unsupported-row detail from “until slice 11” to honest “plugin-delivered Connections remain unavailable; use Settings → Connections.”

**Demonstration (supported path).** Contracted demo:

- Catalog id: `wisp-demo-connection` (name is a choice)
- Covered stdio fixture under `desktop/engine/` (exact filename is an implementation choice), copied by `prepare-product.mjs`
- One MCP tool, raw name `record`, public name `mcp__wispdemo__record`
- Argument schema exactly `{label}` (string `^[A-Za-z0-9_-]{1,40}$`); extra keys fail `strict()`
- Harmless effect: after consume, one `tools/call` that appends one launch-owned 0600 ledger line (`O_NOFOLLOW`), same ownership spirit as 06/10; no network, no user-home writes, no child shell
- Bounded nonsecret config: optional `note` matching `^[A-Za-z0-9_-]{1,40}$` (or empty), shown in native approval fields so configure is observable
- Default: **not installed** so normal-mode inventory stays free of MCP names
- Distinct from `wisp-compatible-plugin` / `wisp_compatible_check` and from developer fixtures
- Hostile fixture (extra unadmitted MCP tool, or `tool-bash` overlay insert) is catalogued as unavailable and never mounted

**Advanced custom MCP (same class, not a second ecosystem).** Advanced UI shows accepted field names from the pin catalog. Wisp-fixed and not user-overridable in this slice: `transport: 'stdio'`, `command`/`args`/`cwd` as above, `failOnStartupError: true`, `reconnect.enabled: false`. User-editable inside the closed envelope: `serverName` (unique, pattern), display label/note, optional protected credential reference. Streamable HTTP, remote URLs, and user-chosen commands stay **unavailable** in Settings (Linux tests may use a loopback HTTP fixture **only** as supplemental endpoint-isolation evidence, not as a shipping connector). One live MCP server maximum.

**Accepted 06 gate (do not weaken).** Native Allow Once / Deny / Cancel Request / Escape/close remain the only action gate. Expand the closed admission table, `permission-protocol.mjs` `validateRequest`, and `PermissionState` allowlists to the new MCP tuple only when that connection is actually loaded; do not pre-admit it when unmounted. Keep existing pairing for developer, compatible-plugin, and 09 safe-action tuples. Decision deadline stays 120 seconds. Queue bound stays 8. Grants remain ephemeral, execution-token-bound, consumed once immediately before the bounded effect (`tools/call`). Unknown/changed arguments, destination, registration, replay, timeout, stale generation, missing facts, and unknown MCP names fail closed. Child `approvalPolicy: never` unchanged. Skills/external children/nested PTC stay disabled. Cordis is not an OS sandbox: this covered fixture is not a reason to mount third-party plugins.

**Unaccepted 07 (record, do not assume).** Historical `07-review-060` HUMAN_REQUIRED is unchanged. Voice is the intended user path (`session/prompt` with recognized text) but is not accepted. 11 must not add a text composer. Committed assistant text remains the only speakable payload; tool calls/results stay out of TTS. Pending native approval still maps to voice phase `approval`. MCP inventory/status events must not be treated as voice-result text. **07 retest** after CompanionController/EngineBridge/VoiceLifecycle/permission-protocol edits: shortcut/Wake, no auto-listen after Apply, mute preserved, no grant/reply revival, no orphan. Do not spend the reserved DeepSeek spoken greeting. Linux must not claim audible proof.

**Parked 08, 09, 10 (record, do not assume working).** Do not implement onboarding, NSWorkspace, or plugin-catalog work beyond the recorded 10 overlay/copy obligation. 09 tools remain Direct. If `product-sdk.ts` inventory/seal or overlay composer grows, record 09 inventory honesty (three safe-action names still present when those sources load; MCP names only when the connection is enabled) and 10 overlay/inventory retest. Do not clear plugin enablement or reasoning keys because a connection ran.

**Linux implementation track (execution-order allowance, not acceptance).** This Linux/cloud host authors JS schema/validators/admission/protocol/stdio-MCP tests and may author native Swift sources. Linux checks do not establish macOS Settings, AppKit, Keychain, shortcut, microphone, or audible behavior. Mocks, doubles, and source inspection are supplemental. Mark Swift uncompiled/unverified unless a compatible runner compiles it. Do not claim macOS Settings or Keychain proof from Linux. Do not mark 07, 08, 09, or 10 Shipped. Do not download models, inspect keys, merge, publish, or spend.

**Persona / inventory.** `product.patch.yml` persona may say that Connections/MCP tools exist when configured and still require Wisp confirmation, and that denied/cancelled tools must not be retried. It is not authorization. `seal()` inventory must equal admitted names. Default normal-mode tools remain the 09 three plus optional developer/compatible fixtures when those inserts are actually loaded; MCP names appear only when the demonstration connection is mounted.

## Data / state impact

Pending MCP requests and unused grants remain process-memory-only; restart restores neither. No durable Allow Always, no remembered MCP allowlist.

New durable state is a versioned, bounded, owner-only connection snapshot under existing application-support (same descriptor-relative, no-follow, revision, directory-lock pattern as reasoning/plugins). It stores catalog id, enabled flag, serverName, nonsecret note, optional opaque credential id, revision — not the MCP server JavaScript (code stays covered product overlay files) and not secret values. Pending Enable/Remove drafts are process memory until Apply. Restart restores the last saved snapshot, not in-flight drafts.

Engine overlay is generated at each attach from `product.patch.yml` + memory insert + optional developer fixture + optional compatible-plugin insert + optional MCP insert. User strings never form YAML syntax. Unexpected extra inserts fail closed. Overlay YAML and `wisp.inventory` never contain secrets.

Optional connection secrets: Mac Keychain service scoped by companion UUID (separate from `com.wisp.reasoning.*`); Linux tests use an isolated 0600 launch-owned file or sentinel env that is not `memory.json`. Injection into mcp-client `config.env` happens in-process in the Wisp adapter so secrets are not written into `--patch` YAML. Pin stdio scrubbing drops ambient `TOKEN`-shaped names; explicit adapter `config.env` is the only supported secret path, and it is still omitted from durable files.

Use fresh `$HOME/wisp-work/wisp-11/<dispatch>/` (or equivalent external) build, support, home, scratch and runtime. Covered docs: `docs/permissions.md` (MCP row becomes the demonstration when mounted), `docs/management-shell.md` Connections copy, `docs/engine-capabilities.md` MCP row, and a new `docs/connections.md` if needed. Sanitized `evidence/11-connections.md` / JSON at implementation. Archives 01–06 and accepted spike bytes remain immutable. Unaccepted 07/08/09/10 copies under `evidence/` stay labeled NOT ACCEPTED.

Identity, selected Wisp home, durable memory, Models route, Voice settings, and the 10 plugin snapshot survive 11 connection use. Enabling a connection does not write `memory.json`.

## Proposed implementation order

After independent APPROVE_PLAN against the coordinator-adopted contract/baseline (no code in this dispatch):

1. **Closed catalog, snapshot, overlay composer, stdio fixture (Linux).** Add connection snapshot schema, static compatibility classifier for the new MCP insert class, and overlay composer that emits one JSON-data insert when enabled. Unit-test: default overlay has no MCP insert and no `mcp__` names in expected inventory; enable emits one insert with validated nonsecret config and Wisp-fixed `failOnStartupError`/`reconnect`; disable omits it; incompatible ids/`npx`/remote URL/unknown fields/oversize notes throw; 10 compatible-plugin insert remains orthogonal; `extraInserts` still reject unexpected extras. Implement the covered stdio fixture with a `tools/call` counter and launch-owned ledger. Extend `prepare-product.mjs` `productFiles` for new sources. Do not fetch npm packages. Do not HTTP-call remote MCP.

2. **Admit + protocol + actual tools/call (Linux).** Wisp adapter `inject: ['tools','wispPermissions']` calls inspected pin `apply`/`startConnection`, then binds the live `mcp__wispdemo__record` definition (wrap `execute` on that same object so `consume()` runs immediately before `callToolUncached`) and `admit`s it with source `wisp-mcp`, revision `1`. Expand `Admission` union, `permission-protocol.mjs`, and native allowlists. `seal()` after `loader.await()`. Expand `wisp.inventory` with connection records; keep 10 plugin records orthogonal. Registry-level withhold/deny/cancel/allow-once: MCP 0/0/0/1 against the real fixture RPC/ledger counters; deny/cancel never increment `tools/call`. Hostile extra MCP tool fails initialize. List-change / `tools/change` after seal: grants revoked, further RPC 0. Credential composition: sentinel in adapter `config.env` for A only; overlay file and B’s env scanned clean. Seal fails if an unadmitted name appears.

3. **Native Swift + Permissions/Connections copy (sources on Linux, uncompiled here).** `ConnectionStore` / `ConnectionsView`, ManagementState Connections copy becomes inventory-backed, SettingsWindowController wiring, confirmation sheets (Enable/Remove). Reuse `stopReasoning` / `startAttachment` generation ownership; pass saved connection snapshot into bridge bootstrap (nonsecret). Expand `PermissionRequest`/`PermissionState`/`PermissionWindow` for `mcp__wispdemo__record`. Permissions copy: demonstration MCP is Ask-each-time when mounted; named SaaS examples, resources/prompts, skills, 15/16 remain unavailable; connection save is not a grant. Mark NativeChecks uncompiled on Linux.

4. **Docs, 06/07/09/10 regressions, identities.** Update permissions/management/capabilities docs. Keep 06 fixture 0/0/0/1 and 09 recording-opener tests. Recheck plugin inventory if overlay composer changes. Recheck voice-seams pending-approval analogue if bridge events change. Recheck identity.py. Hand back proposed proof; independent Reviewer decides. Remaining Mac Settings/Keychain/live/07/10 retest go to the SLICES verification backlog; do not self-approve.

Exact filenames below are routine implementation choices.

## Tests

Required deliverables after plan approval; none claimed run in this no-code phase.

1. **Linux-supplemental JS (must pass on this host).** Extend `sh tools/linux-js-tests.sh` with new tests (names are choices) covering: snapshot schema/unknown-version/oversize/invalid note/serverName; enable/disable composition; no YAML interpolation of user strings; overlay contains no sentinels; 10 classifier still rejects `mcp-connection-plugin` and `@` marketplace specs; permission-protocol accept/reject for `mcp__wispdemo__record` / `wisp-mcp` without breaking existing fixture/09 pairs; withhold/deny/cancel/allow-once 0/0/0/1 on a **real local stdio fixture** (`tools/call` count and ledger); deny/cancel never increment RPC; extra unadmitted MCP tool fails seal; `tools/change` after seal yields zero further RPC; stock ids remain in `DISABLED_STOCK_IDS`; overlay still disables `tool-bash`/`tool-fs`/`tool-web`. Label results Linux-supplemental. They cannot satisfy Mac Settings GUI, Keychain, or live Ollama Done when items.

2. **Isolated product runtime (when a prepared pin runtime exists).** Initialize default composition: inventory includes the three 09 names; excludes `wisp_compatible_check` unless the 10 snapshot is enabled; excludes `mcp__wispdemo__record` and other `mcp__` names; excludes stock bash/fs/web. Enabled connection composition’s inventory includes `mcp__wispdemo__record` and only admitted names. Disable removes it. Hostile extra tool fails initialize. Registry analogue can invoke the MCP tool without a model; 0/0/0/1 against actual `tools/call`. No cloud, no key read, no model download, no `npx`. If the runtime is missing, record gap class **missing-external-resource** for this item and still land Tests 1.

3. **06/07/09/10 regression obligations (Linux analogue + Mac backlog).** Developer fixture and compatible-plugin 0/0/0/1 remain. 09 recording-opener 0/0/0/1 remains; 09 tools are not MCP. Queue bound, stale generation, Apply-during-pending and Quit still close grants including MCP (reuse 06/07 lifecycle oracles). Voice: MCP inventory/ack events cannot enter TTS; pending approval still suppresses speech; stale generation cannot speak. Plugin inventory still fails if enabled demo is missing or extra tools appear. Invalidation: CompanionController receive/Apply/lifecycle, EngineBridge, VoiceLifecycle, permission-protocol, product-sdk admission/inventory, overlay composer. Record 07 retest if those sources change. Record 10 overlay/inventory retest if overlay/inventory/Apply change.

4. **Native assertions (Mac compile; uncompiled on Linux).** `desktop/scripts/test-native.sh` gains: connection snapshot persist/reload; confirm-cancel Enable/Remove performs zero overlay change; unsupported SaaS/plugin-MCP rows not enableable; draft vs saved/active; Apply while busy refused; PermissionRequest decode for `mcp__wispdemo__record`; reject unknown MCP tool names; tab order still Cancel-first; no Allow Always; Permissions copy states connection save is not a grant. `desktop/scripts/build-macos.sh` remains Mac-only here. Keychain tests use an isolated test keychain; never the login keychain; never inspect production keys.

5. **Mac GUI / live (verification backlog, not Linux proof).** On the declared Mac, isolated home: Settings → Connections, keyboard traversal, Enable confirm vs cancel (cancel: zero overlay/RPC), Advanced fields visible and Wisp-fixed values not user-overridable, Apply, restart persistence, Remove. Permissions lists MCP Ask-each-time only after mount. Spoken or developer-prompted request for the demonstration tool → native details show public name, operation, ledger destination (no secret) → Deny/Cancel zero `tools/call` → Allow Once one RPC and one ledger line. Then **07 retest**: shortcut/Wake, no auto-listen, mute preserved, pending-approval speech suppression, no orphan. Live local Ollama 0/0/0/1 when a model is present. NativeChecks pass. Optional Keychain: save/remove opaque connection credential; Diagnostics/memory.json/overlay contain no secret. Do not spend DeepSeek 0/1. Do not sign into GitHub/GDrive/Notion/Calendar/Slack.

6. **Regressions and identities.** Existing `body-bridge`, `permission-protocol`, `reasoning-config`, `memory-context`, `plugin-config`, hardware/ollama inspect, voice-protocol/seams, safe-actions, and spike client tests remain applicable. Pristine pin, byte-identical spike and archives 01–06. No 07, 08, 09, or 10 Shipped claim. Final proof maps each Done when item to Linux-supplemental vs Mac/human evidence.
## Proof
11-plan-075 APPROVE_PLAN consumed. Slice 11 Builder may start against contract 744e61e833cbf3dddbd9ee44ae04de77f6620255f9429fb7f653bb64ce3736f6 and baseline 156f90fad21b2350db7c8b679908bb1b4fdaa9baf426057b8b7ca2b047b0932e (200 files) at HEAD ebbebef33233961d5cfda0e80d7a9af8d53a7161. Not 11 acceptance. Slices 07, 08, 09 and 10 remain implemented with verification pending; historical 07-review-060 HUMAN_REQUIRED unchanged; 08-review-069, 09-review-073 and 10-review-065 IMPLEMENTATION_READY unchanged; no `slices/07-*.md`, `slices/08-*.md`, `slices/09-*.md`, `slices/10-*.md` or `slices/11-*.md`.

Coordinator persisted Builder 11-draft-074 Proposed contract (worker bc-9152c2d3-6075-5ae3-9377-244268458105). No application code in the proposal commits.

## Review
11-plan-075 APPROVE_PLAN — independent reviewer bc-6a6bc2cc-1c95-5e9c-bffd-865b5e8acba2. Contract 744e61e833cbf3dddbd9ee44ae04de77f6620255f9429fb7f653bb64ce3736f6 and candidate 156f90fad21b2350db7c8b679908bb1b4fdaa9baf426057b8b7ca2b047b0932e match before/after (200 files). HEAD ebbebef33233961d5cfda0e80d7a9af8d53a7161. Archives 01–06 untouched. 07, 08, 09 and 10 not Shipped. Not implementation approval. Counters 0/0. Full verbatim $HOME/wisp-work/loop-state/11-plan-075.md. Consumed.

Pending 11-plan-075 plan review of the slice 11 Proposed contract. Consumed.

11-draft-074 Builder draft-proposal complete (bc-9152c2d3-6075-5ae3-9377-244268458105). Handback $HOME/wisp-work/loop-state/11-draft-074.md. Not plan approval. Consumed.

09-review-073 IMPLEMENTATION_READY — independent reviewer bc-9f34474f-ac09-5892-8b0f-d0f09eb30213. Contract 6eb355143ee11ad341064e4bfbba3e12b6d1812aec254e6920a5f63781b35cc6. Linux 109/109 supplemental. Not APPROVE_IMPLEMENTATION. Consumed.

08-review-069 IMPLEMENTATION_READY — independent reviewer bc-e2b4ab93-9bc1-53d4-bff9-ffefcfa8d56c. Contract b08394f1933e5dc1595a1ddff15a49b7a21e462b310b0fc9023782e6fac305d6. Linux 68/68 supplemental. Not APPROVE_IMPLEMENTATION. Consumed.

10-review-065 IMPLEMENTATION_READY — independent reviewer bc-ee11e687-2366-51a9-b9e5-bd56c66c9b01. Contract 7f1000a6a82ea105753ba332957e871dd1b8a10e8ebcce7e9321404cd8ad1636. Linux 49/49 supplemental. Not APPROVE_IMPLEMENTATION. Consumed.

loop-plan-061 APPROVE_PLAN — independent reviewer bc-d950b4e9-45b7-5209-ba88-2af0e6247466. Amendment contract d6689d48dfcf22d42a4d389f7307ebe0b659dc3bf688c706a451eb5d07612c66. Full verbatim $HOME/wisp-work/loop-state/loop-plan-061.md. Consumed.

## Loop state
Tool adapter: Cursor Task generalPurpose subagent; coordinator persists dispatch ID then launches; waits for confirmed completion; Reviewer context is independent of Builder reasoning. One active worker per checkout. This Linux host cannot actuate macOS CUA; Mac physical tests remain participant-gated.
Coordinator: cursor-cloud bc-81f7c99b-f70d-4a0a-83e5-180597760926
Worker / role / phase: none (pending launch) / Builder / Building
Dispatch ID / launch state / input identity: 11-build-076 / pending launch / approved contract 744e61e833cbf3dddbd9ee44ae04de77f6620255f9429fb7f653bb64ce3736f6; baseline 156f90fad21b2350db7c8b679908bb1b4fdaa9baf426057b8b7ca2b047b0932e (200 files)
Pending result / last consumed dispatch: none / 11-plan-075
Snapshot capture and recheck commands / coverage / exclusions: Run `python3 tools/identity.py --repo .` from repository root (replacement of unavailable ../../work/loop-state/identity.py). SHA-256 sorted relative file manifest includes git tracked and non-ignored untracked files, bytes, types, executable bits, symlink targets. Excludes .git, tools/identity.py (hashed only into contract identity), and the WISP_LOOP_STATE store if it lies inside the checkout. BUILD.md and SLICES.md contribute canonicalized bytes: BUILD stops before ## Proof; SLICES omits Run status, Release evidence, Shipped/Now/Later/Implemented-verification-pending placement headings, Implementation ledger and Verification backlog while keeping mapped slice bodies and target membership ordered by slice ID. Contract hashes BUILD through Tests, the Loop-state snapshot capture line, that SLICES canonicalization, full AGENTS/LOOP/BUILDER/REVIEWER, and tools/identity.py bytes. Manifest JSON stored outside coverage at $WISP_LOOP_STATE (default $HOME/wisp-work/loop-state). No other dependency or generated paths currently excluded: place dependencies/test outputs outside checkout. Pinned upstream source remains an external reference; when present verify `git -C "$WISP_HARNESS" rev-parse HEAD` and porcelain status, default $HOME/wisp-work/deepseek-harness; only pristine pin d347e703908d0406b7a7ef80e3a0e594d86b2215 may support source claims. Historical identities below are records only and cannot be recomputed on this host. Changing identity.py rules requires plan re-review.
Baseline snapshot: 156f90fad21b2350db7c8b679908bb1b4fdaa9baf426057b8b7ca2b047b0932e (200 files; approved 11-plan-075 baseline). Parked 09 implementation candidate acf317bbb2645a832daec8db54248cd870c4d69b22b4fbdd5dc393fb630b3714 (199 files) retained as record; parked tree after evidence/09-contract-wip.md was bd000999b8e9156a0620a449cf63fa99b22e45b2195be58b86e9a577b4ff1d30 (200 files, 09 page).
Contract identity: 744e61e833cbf3dddbd9ee44ae04de77f6620255f9429fb7f653bb64ce3736f6 (approved 11-plan-075).
Candidate snapshot: 156f90fad21b2350db7c8b679908bb1b4fdaa9baf426057b8b7ca2b047b0932e (200 files; implementation candidate after 11-build-076).
Rejection count: 0
Consecutive no-progress repairs: 0
Open acceptance gaps / prior failing evidence: Slice 11 implementation not started. 07, 08, 09 and 10 Mac/runtime gates remain backlog and do not block 11. Parked 10 is a recorded obligation, not assumed working. Native Swift uncompiled here. MCP tools must not bypass 06.
Repair awaiting review: false
Review events: 11-plan-075 APPROVE_PLAN contract 744e61e833cbf3dddbd9ee44ae04de77f6620255f9429fb7f653bb64ce3736f6 candidate 156f90fad21b2350db7c8b679908bb1b4fdaa9baf426057b8b7ca2b047b0932e; reviewer bc-6a6bc2cc-1c95-5e9c-bffd-865b5e8acba2; not implementation approval; counters 0/0; verbatim $HOME/wisp-work/loop-state/11-plan-075.md. 11-draft-074 draft-proposal complete builder bc-9152c2d3-6075-5ae3-9377-244268458105; not a review verdict. 09-review-073 IMPLEMENTATION_READY contract 6eb355143ee11ad341064e4bfbba3e12b6d1812aec254e6920a5f63781b35cc6; reviewer bc-9f34474f-ac09-5892-8b0f-d0f09eb30213. 08-review-069 IMPLEMENTATION_READY contract b08394f1933e5dc1595a1ddff15a49b7a21e462b310b0fc9023782e6fac305d6; reviewer bc-e2b4ab93-9bc1-53d4-bff9-ffefcfa8d56c. 10-review-065 IMPLEMENTATION_READY contract 7f1000a6a82ea105753ba332957e871dd1b8a10e8ebcce7e9321404cd8ad1636; reviewer bc-ee11e687-2366-51a9-b9e5-bd56c66c9b01. loop-plan-061 APPROVE_PLAN amendment contract d6689d48dfcf22d42a4d389f7307ebe0b659dc3bf688c706a451eb5d07612c66; reviewer bc-d950b4e9-45b7-5209-ba88-2af0e6247466. 07-review-060 HUMAN_REQUIRED remains the last 07 implementation review (not rewritten). Accepted06 final1/0 archived.
Budget limit / consumed / measurement: no global execution budget. Cloud05 allowance6/6 exhausted and immutable. Separate one-small-agent-cloud-greeting allowance0/1spent, max1024tokens/retry0, requires explicit physicalactivation and ledger reservation beforedispatch. Four user-operated successful connectiontests050 recorded separately, not agent calls. No extra connectiontest/keyinspection/download; local per-scenario modelnoncompliancecap2. Linux host must not consume the reserved cloud greeting.
Blocker / resume status / resume action / recheck condition / deadline: none for slice 11 Building. Recheck: Builder 11-build-076 completion. No automatic paid calls/downloads. Counters0/0.
Advance phase: none
Next slice ID / draft: 11 / implementing

## Status
Building

## Next
Independent Builder 11-build-076 against approved 11-plan-075. Do not mark 07, 08, 09, or 10 Shipped. Counters 0/0.
